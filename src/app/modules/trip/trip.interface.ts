export type TPhoto = {
  url: string;
  isDeleted: boolean;
};

// export interface UpdateTripPayload extends Partial<Omit<Trip, 'photos' | 'activities'>> {
//   photos?: TPhoto[];
//   activities?: string[];
// }
